
nginx:
  image: 'ignore, overridden at build time'
drupal:
  image: 'ignore, overridden at build time'
  env:
    ENV_VAR_1: 'env-var-one-value'
    ENV_VAR_2: 'env-var-two-value'

mariadb:
  persistence:
    storageClass: standard
  replication:
    enabled: false

  db:
    name: drupal
    user: drupal

ingress:
  hostname: "silta.wdr.io"
