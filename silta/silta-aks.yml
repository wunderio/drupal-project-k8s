# Configure reference data that will be used when creating new environments.
referenceData:
  enabled: true
  referenceEnvironment: 'feature/aks-cluster/reference'

# Should be overriden by CI
cluster:
  type: aks

# AKS does egress NAT by default
silta-release:
  proxy:
    enabled: false

### Azure Application Gateway Ingress
# exposeDomains:
#   example-aks-ingress:
#     hostname: aks-ingress-test.silta-aks.wdr.io
#     ingress: aks

# ingress:
#   aks:
#     type: azure/application-gateway

### CSI Storage overrides for AKS / Filestore

# shell:
#   mount:
#     storageClassName: azurefile-csi
#     storage: 50M

# mounts:
#   public-files:
#     storageClassName: azurefile-csi
#   private-files:
#     storageClassName: azurefile-csi

# referenceData:
#   storageClassName: azurefile-csi

# backup:
#   storageClassName: azurefile-csi
