# Configure reference data that will be used when creating new environments.
referenceData:
  enabled: false

varnish:
  enabled: false

memcached:
  enabled: false

# TODO: Figure out backendConfig defaults for Drupal service
backendConfig: null

cluster:
  type: aws

silta-release:
  proxy:
    enabled: false
